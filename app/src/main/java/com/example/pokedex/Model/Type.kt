package com.example.pokedex.Model

class Type {
    /**public Type(int slot, Species type){
     * this.slot = slot;
     * this.type = type;
     * } */
    var slot = 0
    var type: Species? = null

}